# Django Settings
SECRET_KEY=your-secret-key-here-generate-with-python -c "import secrets; print(secrets.token_hex(50))"
DEBUG=False
ALLOWED_HOSTS=your-app-name.onrender.com,localhost,127.0.0.1

# Database (leave empty for SQLite locally, Render will provide DATABASE_URL)
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Email Settings (for password reset)
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# CORS origins for your frontend
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.onrender.com
